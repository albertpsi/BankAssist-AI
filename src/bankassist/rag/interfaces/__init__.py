"""Protocols for stages that have — or will have — more than one implementation.

Stages with exactly one implementation (``HybridRetriever``, ``MetadataFilter``,
``RRFRanker``) have no Protocol here, per CLAUDE.md's "no interface before a
second implementation" rule.
"""
